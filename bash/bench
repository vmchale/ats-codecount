#!/usr/bin/env bash

file="test/data/sqlite3.c"
bench "./target/wc-demo $file" \
    "loc -u $file" \
    "tokei --no-ignore-vcs --no-ignore $file" \
    "scc -c --no-cocomo --binary --no-gitignore --no-ignore $file" \
    "cloc $file" \
    "lang ${file}"
